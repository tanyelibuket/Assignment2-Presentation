name: GitHub Pages CI

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify Pages content
        run: |
          echo "GitHub Pages repository detected"
          ls -la
          echo "CI workflow executed successfully"
